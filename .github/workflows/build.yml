name: Build RustDesk Client

on:
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout rustdesk (your fork)
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          path: rustdesk
          
      - name: Checkout hbb_common (your fork)
        uses: actions/checkout@v4
        with:
          repository: 6579080/hbb_common
          path: rustdesk/libs/hbb_common
          
      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc
          
      - name: Build Windows Client
        run: |
          cd rustdesk
          cargo build --release --target x86_64-pc-windows-msvc
          
      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-windows
          path: rustdesk/target/x86_64-pc-windows-msvc/release/rustdesk.exe
